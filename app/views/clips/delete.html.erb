<div class="container">

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@clip, :method => 'delete') do |f| %>
        Are you sure you want to delete this clip?
        <%= f.object.clip_name %>
        <%= f.submit("Delete clip") %>
    <%end %>
  </div>
</div>

</div>
